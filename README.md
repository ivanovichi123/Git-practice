An exercise to practice git.
Git --ammend: Use it when forget to include a file, add the new file and then commit this will replace the old one with the new one
git rebase -i HEAD~2: Use it to change various commits
squash: Combine two commits into one